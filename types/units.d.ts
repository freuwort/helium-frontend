export type BaseUnit = {
    code: string
    symbol: string
    name: string
}

export type Currency = {
    code: string
    symbol: string
    decimal_places: number
    name: string
}

export type Country = {
    code: string
    alpha_3: string
    name: string
}