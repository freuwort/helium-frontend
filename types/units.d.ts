export type Unit = {
    code: string
    symbol: string
    name: string
    type: string
}

export type Currency = {
    code: string
    symbol: string
    decimal_places: number
    name: string
}

export type Country = {
    code: string
    alpha_3: string
    name: string
}